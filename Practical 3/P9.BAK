#include<stdio.h>
#include<conio.h>
void main()
{
	char str[10];
	int length;
	printf("Enter string:");
	gets(str);
	length=0;
	while(str[length]!='\0')
		length++;
	printf("string %d",length);
	getch();
}
